# PetalSonic 自动音频运行时与深模块架构规格

## Problem Statement

Re:Flora 需要把 PetalSonic 当作一个可靠的游戏音频系统使用：调用方提交声源、播放意图和场景状态后，PetalSonic 应自行持续生成声音，并在声卡切换、设备暂时不可用和游戏线程抖动时保持可恢复。当前架构没有兑现这一边界。音频渲染进度依赖调用方显式调用 `pump_audio()`，而仓库中的 Demo 没有执行该调用，导致设备回调只有消费者而没有音频生产者。

与此同时，当前核心引擎同时承担设备管理、平台适配、渲染调度、命令处理、混音、空间音频、重采样、缓冲和事件传递。公共 API 还暴露了内部命令、播放实例和空间处理器类型，使调用方可以绕过线程所有权及实时约束。高频状态、控制命令、事件和计时信息通过无界通道传递，设备回调中存在格式化错误字符串等不符合实时约束的行为。

这些问题使 PetalSonic 的实际使用契约隐含、模块边界变浅、故障恢复脆弱，也让测试难以从调用方视角证明声音一定会持续推进。由于 PetalSonic 是私有库，本次架构调整不受历史 API 兼容性约束，但仍应只引入能明确降低复杂度、提高可靠性或建立可测试契约的重设计。

## Solution

PetalSonic 将成为完全拥有自身音频进度和设备生命周期的深模块。每个 `PetalSonicWorld` 拥有一个独立的音频运行时，在 World 创建时启动专用渲染线程和设备管理，在显式关闭或销毁时停止并等待线程退出。Re:Flora 不再调用公开的 pump 接口；它只提交播放控制、总线意图和最新场景状态，并拉取事件及诊断信息。

正常调用路径只要求 Re:Flora 管理一个不透明的 Emitter 句柄。PetalSonic 内部仍将长期存在的空间发声体与单次播放 Voice 分开管理，使生命周期明确且允许一个 Emitter 同时播放多个声音。普通单次播放由 PetalSonic 自动回收；只有确实需要单独暂停、停止或查询时，调用方才显式申请 `PlaybackControl`。

高频的监听者和 Emitter 空间状态将按游戏帧组成一个原子 `SpatialFrame`，音频运行时只消费最新完整版本。创建、播放、停止、销毁等不可静默丢失的操作使用独立的有界非阻塞控制通道。输出事件由 Re:Flora 每帧主动拉取，实时线程绝不回调游戏代码。

空间音频处理方案在 World 创建时由面向效果的质量配置确定，并在 World 生命周期内保持不变。原生 HRTF、Ambisonics、传播与混响的具体组合属于 PetalSonic 内部实现。声学几何通过不可变的 `AcousticSceneSnapshot` 发布：稳定运行时只交换轻量版本引用，几何变化时在音频线程之外增量构建。PetalSonic 自己拥有一个限频、限射线、只消费最新完整输入的传播 worker；音频渲染线程只交换已完成的声学响应，不遍历宿主几何，旧版本也不在音频线程回收。

PetalSonic 的逻辑设备输出明确为双声道。默认调度采用有低水位和高水位的平衡型预渲染策略，目标约为两个设备周期。系统默认输出设备发生变化或暂时不可用时，PetalSonic 保留完整 World 状态并自动恢复；它尽可能先打开和预填充新设备，再释放旧设备。硬件或操作系统可能造成短暂物理间隙，但不会要求 Re:Flora 重建音频世界，也不会让音频永久失效。

本轮播放资源限定为预解码、不可变且可共享的常驻 PCM。长音乐和长环境音的流式解码只预留独立模块边界，不进入本次实时渲染核心。混音公共模型限定为创建时声明的少量音频总线统一汇入 Master，不提供任意运行时 DSP 图。

## User Stories

1. 作为 Re:Flora 开发者，我希望创建 `PetalSonicWorld` 后音频能够自行持续渲染，从而不必在游戏循环中维护隐藏的 pump 义务。
2. 作为 Re:Flora 开发者，我希望 PetalSonic 自己拥有渲染线程，从而避免游戏帧抖动直接停止音频生产。
3. 作为 Re:Flora 开发者，我希望销毁 World 时其线程和设备流能够确定地关闭，从而不会遗留后台线程或声卡资源。
4. 作为 Re:Flora 开发者，我希望不同 World 的音频运行时彼此隔离，从而测试或工具实例不会依赖全局单例。
5. 作为游戏逻辑开发者，我希望正常情况下只管理一个 Emitter 句柄，从而不必同时维护空间声源和每次播放的两套标识符。
6. 作为游戏逻辑开发者，我希望一次普通播放结束后自动回收内部 Voice，从而不必为短音效编写清理代码。
7. 作为游戏逻辑开发者，我希望同一个 Emitter 可以同时播放多个声音，从而能够表达脚步、机械运转和碰撞声等重叠效果。
8. 作为游戏逻辑开发者，我希望只有在需要控制某次具体播放时才申请 `PlaybackControl`，从而让简单路径保持简单。
9. 作为游戏逻辑开发者，我希望销毁 Emitter 时其空间状态和关联播放具有明确的清理规则，从而不会留下不可见的声音或资源。
10. 作为游戏逻辑开发者，我希望通过自己的业务标签识别受控播放的完成事件，从而不必保存 PetalSonic 的内部 Voice 标识符。
11. 作为 Re:Flora 场景系统开发者，我希望每帧一次性提交监听者和 Emitter 的最新空间状态，从而避免大量过时位置命令堆积。
12. 作为玩家，我希望同一游戏帧内的监听者与声源变换一起生效，从而不会听到由半新半旧场景状态造成的空间跳变。
13. 作为 Re:Flora 开发者，我希望高频空间更新可以覆盖旧版本，从而突发负载不会产生不断增长的音频延迟。
14. 作为 Re:Flora 开发者，我希望播放、停止、创建和销毁操作不会被静默丢弃，从而游戏状态与声音状态保持可解释。
15. 作为 Re:Flora 开发者，我希望所有命令提交都是有界且非阻塞的，从而音频系统不会卡住游戏线程。
16. 作为 Re:Flora 开发者，我希望队列过载返回明确的类型化结果，从而可以记录、重试或降级，而不是猜测命令是否生效。
17. 作为玩家，我希望游戏线程偶发卡顿时声音仍然连续，从而不会出现可以避免的爆音和断音。
18. 作为玩家，我希望音频交互保持足够灵敏，从而预渲染余量不会造成明显的游戏反馈迟滞。
19. 作为 Re:Flora 开发者，我希望通过面向体验的延迟档位选择调度策略，从而不必硬编码平台相关的缓冲帧数。
20. 作为使用耳机的玩家，我希望切换到音箱或其他系统默认输出设备后游戏声音自动恢复，从而不必重启游戏或重建场景。
21. 作为玩家，我希望输出设备短暂消失后 PetalSonic 持续重试，从而设备重新可用时声音能够自行返回。
22. 作为玩家，我希望设备恢复后循环声音位于当前游戏时间点，从而不会从断开前的位置重播陈旧内容。
23. 作为玩家，我希望设备不可用期间已经过期的短音效不会集中补播，从而恢复时不会出现一阵失真的声音爆发。
24. 作为 Re:Flora 开发者，我希望设备切换不销毁 Emitter、Voice、总线和空间状态，从而恢复行为不需要重新创建整个音频世界。
25. 作为 Re:Flora 开发者，我希望能够观察 `Running`、`Recovering` 和 `Failed` 等运行状态，从而 UI 和诊断系统可以准确表达音频健康状况。
26. 作为平台维护者，我希望设备错误回调只记录轻量原子状态，从而声卡回调中不会发生字符串格式化、日志写入或无界分配。
27. 作为 Re:Flora 开发者，我希望每帧主动拉取播放完成、恢复和诊断事件，从而所有游戏代码都在已知线程执行。
28. 作为音频系统维护者，我希望 PetalSonic 永远不从实时线程进入用户回调，从而游戏锁、日志和未知耗时不会破坏音频时限。
29. 作为游戏设计师，我希望把声音分配到 Gameplay、Music、UI 和 Voice 等总线，从而可以分别控制音量、静音和暂停。
30. 作为玩家，我希望暂停游戏时游戏音效停止推进而菜单音乐可以继续，从而暂停行为符合内容类别的预期。
31. 作为游戏设计师，我希望慢动作只影响选定总线或 Voice，从而底层设备时钟和 UI 声音仍保持稳定。
32. 作为音频内容开发者，我希望所有本轮支持的音频在播放前完成解码，从而解码和文件 I/O 不会进入实时渲染路径。
33. 作为音频内容开发者，我希望多个 Voice 能共享同一份不可变 PCM 资源，从而重复音效不会重复占用解码内存。
34. 作为 Re:Flora 开发者，我希望逻辑输出契约始终是双声道，从而资源、混音、重采样和设备回调对声道布局有一致理解。
35. 作为空间音频使用者，我希望通过 `LowLatency`、`Balanced` 或 `HighQuality` 一类效果档位配置空间声音，从而不必理解具体 DSP 后端。
36. 作为空间音频维护者，我希望一个 World 的处理后端在创建后保持稳定，从而播放过程中不存在后端热切换的状态组合和资源迁移问题。
37. 作为 PetalSonic 维护者，我希望空间效果由原生算法实现，从而公共接口和生命周期不绑定第三方声学运行时。
38. 作为 Re:Flora 世界系统开发者，我希望发布一个不可变声学场景版本供音频查询，从而音频线程不会锁住或直接遍历可变游戏世界。
39. 作为 Re:Flora 世界系统开发者，我希望未变化的静态几何和 BVH 在快照之间共享，从而发布新版本不需要深拷贝整个场景。
40. 作为 Re:Flora 世界系统开发者，我希望只重建发生变化的声学分块，从而动态场景更新成本与变化范围相关而不是与世界总规模相关。
41. 作为音频线程维护者，我希望旧声学场景版本在非音频线程回收，从而最后一个引用释放不会触发实时路径内存回收。
42. 作为 PetalSonic API 使用者，我希望系统能力集中在 `PetalSonicWorld` 门面和少量不透明值对象中，从而不会误用混音器、播放实例或空间处理器内部接口。
43. 作为 PetalSonic 维护者，我希望内部 Emitter 与 Voice 身份使用可检测陈旧引用的代际标识，从而复用槽位时不会操作错误对象。
44. 作为 PetalSonic 维护者，我希望最大 Emitter、Voice、总线和队列容量都有实际约束，从而配置项不会只存在于表面而运行时仍无限增长。
45. 作为 PetalSonic 维护者，我希望混音、空间处理和设备输出复用预分配缓冲区，从而稳定渲染期间不产生不可控分配。
46. 作为平台维护者，我希望 CPAL、Linux 设备探测和 unsafe 生命周期集中在明确适配层，从而其不变量能够被单独审查。
47. 作为测试作者，我希望用假声卡、可控时钟和可捕获输出驱动完整 World，从而不依赖真实硬件或休眠就能复现时序场景。
48. 作为测试作者，我希望从公开行为验证设备切换、过载、暂停和回收，从而测试不会因内部线程或模块重排而失效。
49. 作为 DSP 维护者，我希望原生 HRTF、Ambisonics 和反射算法继续拥有数值及属性测试，从而深模块内部重构不会改变可听行为。
50. 作为发布维护者，我希望发布流程运行格式检查、静态检查、全部测试和关键基准，从而架构契约不会在发布包中退化。
51. 作为示例程序使用者，我希望 Demo 只使用正式公共 API 就能持续出声，从而示例能够证明真实集成路径完整。
52. 作为文档读者，我希望所有文档都描述引擎拥有的渲染线程和自动设备恢复，从而不会继续遵循已经移除的旧线程或 pump 契约。
53. 作为 PetalSonic 维护者，我希望移除没有生产者、没有调用方或没有实际约束的旧接口与类型，从而代码规模反映真实能力。
54. 作为架构维护者，我希望模块依赖从公共门面单向流向运行时、混音、空间处理和平台适配，从而播放与空间模块不再通过 World 类型相互循环引用。

## Implementation Decisions

- **公共系统边界**：`PetalSonicWorld` 是唯一的系统门面。公共面只保留创建描述、不可变音频资源、Emitter 句柄、可选的 `PlaybackControl`、总线句柄、事件、状态快照和类型化错误。内部命令、播放实例、混音器、空间处理器、原生 DSP 类型及命令接收端不再公开。

- **兼容策略**：本库为私有库，本次允许删除或替换旧 API，不提供兼容适配层。迁移以减少概念数量和建立单一正确路径为优先，但已有且足够深的原生 DSP 模块不因“重构”而被机械拆分。

- **运行时所有权**：每个 World 独占一个 `AudioRuntime`。运行时拥有专用渲染线程、设备管理、逻辑音频时钟、环形输出缓冲和内部通信端点。不存在进程级全局运行时，也不跨 World 共享工作线程。

- **生命周期**：World 创建时启动运行时；显式关闭负责停止接收新工作、终止设备流、请求渲染线程退出并等待完成。World 的兜底析构执行同样的有界清理。没有输出设备不是配置错误：配置有效时 World 可进入 `Recovering` 并等待设备出现。永久不受支持的格式或后端初始化失败才产生不可恢复错误。

- **删除公开 pump**：`pump_audio()` 不再属于公共 API。内部保留单步渲染原语，供渲染线程、确定性测试、基准和未来可能的离线渲染模块使用；调用方不能通过它建立第二套调度模式。

- **模块边界与依赖方向**：公共领域类型不依赖运行时实现；World 门面只通过受限端口提交意图并读取状态；运行时协调混音、空间渲染和平台输出；播放与空间模块共享最小化的内部 ID 和渲染描述，不相互导入完整实例；平台适配和第三方 FFI 位于依赖图底部；原生 DSP 算法作为叶子深模块存在。

- **Emitter 与 Voice**：内部使用长期 `EmitterId` 和单次 `VoiceId` 两类代际标识。Emitter 保存空间及默认播放参数；Voice 保存资源引用、播放游标、循环、增益、总线和可选 Emitter 绑定。一个 Emitter 可以拥有多个 Voice。普通播放不向调用方暴露 Voice 标识并在结束后自动回收。

- **外部单句柄路径**：Re:Flora 正常只持有 Emitter 句柄。创建、播放、更新默认参数、停止全部声音和销毁均通过 World 门面完成。只有显式选择受控播放时才返回 `PlaybackControl`；丢弃控制对象不会隐式阻塞或在析构中执行可能失败的关键命令，停止仍是显式操作。

- **Emitter 清理语义**：显式销毁 Emitter 后，不再接受针对该句柄的新操作；其空间状态立即从下一个可见版本移除，仍绑定的 Voice 默认停止并执行短消爆音斜坡。需要在实体消失后继续播放的声音必须在播放时显式声明为 detached，而不是依赖偶然生命周期。

- **容量契约**：用可执行的 `max_emitters`、`max_voices`、`max_buses` 和队列容量替代没有实际作用的容量字段。相关表和热路径缓冲在初始化时预分配；超过容量返回 `CapacityExceeded`，不在实时路径扩容。

- **控制通道**：创建、销毁、播放、停止、总线控制和其他离散操作使用有界、非阻塞控制通道。提交调用只返回“已接受”或具体拒绝原因，不等待音频线程完成。关键生命周期操作拥有保留容量；即使过载也不得静默丢弃。跨生产者的全局顺序不作为隐含保证，需要顺序的操作由同一 World 控制线程提交。

- **空间状态通道**：监听者和动态 Emitter 状态组成带单调 revision 和模拟时间戳的完整 `SpatialFrame`。Re:Flora 每个游戏帧最多发布一个版本；发布是有界的，未消费的旧版本可被新版本覆盖。音频处理量子和传播 worker 都只观察一个完整版本，避免监听者和声源状态撕裂。创建和销毁仍由控制通道定义生命周期，快照不能隐式创建未知 Emitter。

- **声学场景通道**：静态及低频变化几何通过不可变 `AcousticSceneSnapshot` 版本发布。版本切换为 O(1) 的句柄或索引交换，不是每帧深拷贝。未变化数据结构共享；变化分块在非音频线程构建。传播 worker 使用一个稳定版本完成每次限额求解，并把带空间 revision 与几何 version 的响应发布给渲染线程；旧版本由非实时线程释放。

- **传播预算**：原生传播 worker 约以 30 Hz 消费最新完整输入，直接声最多选择 32 个按业务优先级和距离排序的 Emitter，晚期混响使用固定 256 条射线和 8 次反弹。持续更新不能形成无界队列；求解期间出现的新输入会在下一轮取代旧输入，最近一个完整响应仍可使用而不会因 60 Hz 游戏帧导致永久饥饿。

- **Re:Flora 几何复用**：如果 Re:Flora 已有不可变且线程安全的碰撞 BVH，PetalSonic 通过只读声学查询适配层共享它，并附加声学材质映射；只有格式无法满足声学查询时才建立独立派生加速结构。音频线程不得访问可变 ECS、物理世界锁或需要分配的用户逻辑。

- **空间渲染深模块**：运行时只依赖一个窄而深的 `SpatialRenderer` 契约，输入为当前监听者、有效 Voice/Emitter 渲染描述、声学场景版本和复用缓冲，输出为双声道混音贡献及有界指标。原生 HRTF、Ambisonics、反射和混响的资源及多阶段流水线封装在该 seam 之后。

- **后端选择**：公共配置表达 `LowLatency`、`Balanced`、`HighQuality` 等效果目标和必要能力，不公开 HRTF、Ambisonics、直接声、反射等阶段的实现开关。World 创建时解析出一个固定处理方案；活动播放期间不热切换。开发 A/B 通过内部测试配置或重建 World 完成。

- **逻辑输出格式**：混音器、空间渲染器和内部输出环统一产生双声道浮点帧。删除没有兑现的公共 `channels` 配置。平台输出适配器负责交错、样本格式转换以及必要的物理设备声道映射，但不得把设备声道布局反向泄漏到核心混音契约。

- **渲染调度**：默认 `Balanced` 档位以约两个设备周期为目标余量。运行时维护低水位和高水位：低于低水位时补充到高水位，并根据设备周期及实测渲染时间计算唤醒节奏。`Responsive` 与 `Robust` 只改变受约束的策略参数，不允许调用方传入未经验证的任意缓冲帧数。

- **实时回调规则**：设备回调只读取预生成的双声道帧、执行必要格式写入、更新原子计数或设置原子错误标志。回调中禁止内存分配和释放、字符串格式化、日志、阻塞锁、文件或设备查询、用户回调以及无界通道发送。环形缓冲欠载时输出静音并增加欠载计数。

- **渲染线程规则**：渲染线程不依赖游戏线程进度，不执行文件 I/O 或解码，不调用可变宿主世界，并使用复用缓冲完成命令应用、Voice 推进、空间处理、混音、重采样和输出生产。与第三方库交互必须具有可审查的最大工作量或明确的质量降级策略。

- **播放时钟**：底层设备和 Voice 推进使用 PetalSonic 的单调音频时钟，不使用游戏帧 delta 驱动。Re:Flora 显式控制总线暂停、恢复和播放速率。暂停总线冻结其 Voice 进度；设备暂时不可用则不冻结全局时间线，循环声音在恢复时位于当前进度，已经结束的一次性声音不补播。

- **总线模型**：World 创建时声明少量稳定总线，所有总线直接汇入 Master；运行时只更新音量、静音、暂停和播放速率等参数。Emitter 和 Voice 可选择总线。第一版播放速率通过重采样改变速度并伴随音高变化；高质量保音高时间拉伸不属于本次范围。公共 API 不支持任意节点、发送、侧链或运行时拓扑编辑。

- **常驻 PCM 资源**：本轮可播放资源必须在进入运行时前完成解码，作为不可变、可共享的 `ResidentClip` 存在。文件访问、格式识别和解码失败发生在非实时资源加载边界。多个 Voice 共享底层 PCM；实时线程不懒加载、不解码也不触发资源释放的最后引用。

- **设备选择**：默认策略为跟随系统默认输出设备；可选的显式固定设备策略保持固定，除非该设备暂时失效并按其恢复策略重连。设备枚举、默认设备检测和平台特殊处理由独立输出平台适配器负责。

- **设备切换与恢复**：设备管理在非实时线程检测默认输出身份变化，不能只依赖旧流报错。切换时尽可能先打开新流、协商格式、重建输出重采样状态并预填充缓冲，再关闭旧流；两端允许并存时执行短交叉淡化，否则恢复后执行短淡入以避免爆音。设备采样率或物理声道布局变化不重建 World。

- **恢复状态机**：运行时状态至少区分 `Running`、`Recovering`、`Failed` 和关闭过程。暂时无设备、设备被拔出和默认设备切换进入 `Recovering`，立即尝试新设备，后续使用有上限的退避间隔并持续重试。只有静态配置无效、必要后端无法初始化或明确不可恢复的错误进入 `Failed`。

- **事件与状态**：Re:Flora 通过 `drain_events()` 在游戏线程拉取有界事件，并可读取当前状态及累计诊断快照。状态型变化可以合并；受控播放完成事件只在调用方要求时产生并携带业务标签。事件队列压力本身必须可观测，不能用无限增长维持“无丢失”假象。

- **诊断数据**：提供无分配或快照化的欠载次数、渲染耗时分位信息、队列高水位、拒绝命令数、活动 Emitter/Voice 数、设备代次、恢复次数和空间后端配置。实时路径只更新预分配计数；字符串化和日志输出在调用方线程完成。

- **类型化错误**：公共错误区分配置无效、容量耗尽、队列压力、陈旧句柄、运行时关闭、后端不支持和永久设备失败。面向调用方的 API 不以通用动态错误作为稳定契约，也不在设备回调中构造错误对象。

- **unsafe 与 FFI 边界**：第三方 C 回调上下文由拥有它的运行时或空间后端显式持有，并保证先停止所有可能回调的工作再释放；不得通过永久泄漏堆对象解决生命周期。任何 `Send`/`Sync` unsafe 声明都必须附带可审查不变量，并尽可能由所有权结构替代。平台探测和 Linux 特例不得散落在核心引擎。

- **删除与收敛**：实现阶段应通过删除测试确认并移除没有调用方的填充回调接口、未构造的音频源包装类型、没有生产者的事件变体、未使用依赖、过时的内部线程说明和重复的构建锁定产物。容量字段若保留必须变成真实约束；不能继续保留“将来可能有用”的空接口。

- **文档与示例**：所有公共文档和 Demo 只展示自动运行的 World、Emitter、总线、空间帧、事件拉取和恢复状态。删除任何关于调用方 pump 或已不存在渲染线程模型的描述。示例必须能作为最小真实集成测试，而不是绕过公共 API。

- **构建与发布**：发布门槛包含格式检查、静态检查、工作区全部目标测试、Demo 构建、支持平台的设备适配编译检查以及关键性能基准对比。发布脚本不能只检查编译而跳过测试；平台相关验证结果应明确记录。

## Testing Decisions

- **主要测试接缝**：唯一首选高层接缝是 `PetalSonicWorld` 的公开行为。测试使用同一套公共创建、Emitter、播放、空间帧、总线、事件和状态接口，内部连接可控单调时钟、假输出设备和可捕获双声道帧。测试不直接驱动公开 pump，也不读取内部命令或处理器。

- **好测试的定义**：测试断言调用方可观察到的声音帧、事件、状态、错误、恢复和资源生命周期，不断言线程名称、私有队列类型、具体唤醒次数或内部后端对象布局。内部模块重排不应要求重写高层契约测试。

- **自动进度测试**：创建 World、Emitter 和 ResidentClip 后只提交播放，不调用任何泵；推进可控设备时钟后必须观察到非静音双声道帧。Demo 的同类集成路径也必须通过正式 API 出声。

- **World 生命周期测试**：验证创建会启动可用运行时，显式关闭会停止接受命令并完成线程回收，重复关闭安全，多个 World 不共享状态，一个 World 的关闭不影响另一个。

- **Emitter/Voice 测试**：验证普通 Voice 自动回收、同一 Emitter 可重叠播放、受控播放可单独停止、业务标签随完成事件返回、陈旧 Emitter/PlaybackControl 被拒绝、销毁 Emitter 会按规定停止绑定 Voice，而 detached Voice 可继续。

- **空间帧测试**：连续发布多个未消费 `SpatialFrame`，只允许最新完整版本生效；监听者和所有 Emitter 必须来自同一代次。大量移动 Emitter 不得造成控制队列或内存随帧数增长。

- **控制过载测试**：使用极小的测试容量填满控制通道，验证调用永不阻塞、状态更新可合并、生命周期命令不会静默成功、拒绝原因稳定且队列恢复后可继续工作。

- **容量测试**：分别达到并超过 Emitter、Voice、总线和事件容量，验证成功边界、明确错误、槽位释放后的代际安全以及稳定状态下没有隐式扩容。

- **时钟与总线测试**：验证设备时钟独立于游戏帧；暂停 Gameplay 总线冻结其 Voice 而 Music 继续；恢复后从冻结位置继续；播放速率只影响目标总线，并遵循第一版速度与音高共同变化的语义。

- **缓冲调度测试**：假设备以不同帧数和周期请求数据，验证低/高水位调度能够补充输出、欠载时只输出静音并增加计数、恢复后不会补写过期帧。测试使用虚拟时间，不依赖真实休眠。

- **设备切换测试**：模拟默认设备 A 仍可用但系统默认变为 B，验证运行时主动打开 B、预填充并完成代次切换；模拟 A 突然断开、短暂无设备和随后出现 B，验证状态依次进入 `Recovering` 和 `Running`，World/Emitter/Voice 不被重建。

- **恢复时间线测试**：设备不可用期间推进虚拟时间，验证循环 Voice 恢复到当前进度，一次性 Voice 正常完成且不在恢复后补播，受控完成事件仍遵循其通知契约。

- **永久失败测试**：模拟不受支持的静态格式、后端初始化失败和持续可恢复设备缺失，验证前两者进入明确失败，后者保持有界退避的 `Recovering`，二者不会混淆。

- **事件测试**：验证 `drain_events()` 只在调用线程执行用户逻辑，状态事件按规则合并，完成事件携带业务标签，队列压力可观测，设备回调和渲染线程从不调用测试提供的用户函数。

- **实时安全测试**：为设备回调安装测试分配器和锁探针，验证稳态和错误路径都没有分配、释放、日志、格式化或阻塞锁。相同探针用于确认声学旧版本的最后释放不发生在音频线程。

- **传播测试**：用确定性的解析房间和开放空间实现 `AcousticRayQuerySnapshot`，验证输入空间 revision 与几何 version 成对、开放空间不凭空产生混响、三频段衰减有界、非法宿主数值不会把 NaN 送入 DSP、最新输入最终可见且 worker 可确定关闭。端到端性能结论仍以宿主游戏的 release 模式运行和日志为准。

- **声学快照测试**：发布共享静态几何的多个版本，验证音频处理量子只观察一个版本；局部分块更新不复制未变化分块；旧版本在音频线程离开后由非实时端回收。

- **空间渲染契约测试**：原生空间模块运行一组输入/输出契约测试，包括无声、单声源方位、距离衰减、遮挡、反射开关、最大源数和确定性边界。公共质量档位只验证可观察能力，不暴露内部阶段组合。

- **DSP 既有先例**：继续使用仓库现有原生 HRTF、Ambisonics、反射、混音和重采样单元测试作为数值先例；新增属性测试覆盖有限输出、无 NaN/Inf、左右对称性、能量边界和分块尺寸变化。低层测试补充而不替代 World 高层契约测试。

- **平台适配测试**：用假平台实现覆盖设备枚举、默认身份变化、采样率变化、多物理声道映射、流错误和关闭顺序；真实 Linux、Windows 和 macOS 设备只承担少量烟雾测试，不能成为确定性测试的唯一证据。

- **压力与长稳测试**：长时间运行最大配置附近的 Emitter/Voice、频繁空间快照、设备切换和总线更新，验证内存有界、线程数稳定、事件压力可见且没有死锁。测试结束后所有 FFI 回调上下文和设备资源必须可回收。

- **性能基准**：建立单步内部渲染基准，分别测量混音、空间处理、声学查询、重采样和完整量子；记录 P50/P95/P99、最大值、活动 Voice 数和声学射线数。`Balanced` 配置的 P99 必须在目标设备周期预算内保留明确余量，具体阈值由首轮跨平台基线确定并固化。

- **删除测试**：在删除旧接口前使用全仓库符号搜索、所有目标编译和文档测试证明无调用方；删除后重新运行工作区测试、Demo 构建和发布检查。对于旧事件变体，先证明没有生产者和消费者。

- **发布验收**：格式检查、静态检查、工作区全部目标测试、Demo 自动出声集成测试、关键基准无显著回退以及支持平台适配编译必须同时通过。任何实时回调分配或设备切换后 World 失效都是阻断发布的问题。

## Out of Scope

- 不实现长音乐或长环境音的流式读取、后台解码、寻址和每流欠载策略；只预留未来独立 `StreamingSource` 模块的边界。
- 不支持 5.1、7.1 或其他逻辑多声道输出；PetalSonic 核心输出固定为双声道，物理设备适配属于平台层。
- 不提供公开的手动 `pump_audio()`、第二套合作式调度模式或调用方拥有的渲染线程。
- 不公开逐阶段后端组合开关；运行时环境声学启停属于有界、平滑的效果控制，而不是更换实现。
- 不提供任意运行时 DSP 图、用户自定义节点、发送、侧链或动态总线拓扑。
- 不实现保音高时间拉伸；第一版播放速率变化允许同时改变音高。
- 不允许音频线程直接访问可变 Re:Flora ECS、物理世界或需要锁定的实时游戏对象。
- 不保证输出设备物理切换绝对零间隙；保证的是 World 不失效、状态不丢失并在设备可用后自动恢复。
- 不建立跨 World 的全局混音、共享设备单例或跨 World Voice 路由。
- 不为历史公共 API、内部类型导出或旧线程模型提供兼容层。
- 不在本规格中规定具体 Rust 文件布局、类型字段或第三方库版本；模块边界和行为契约优先于易过时的文件级方案。
- 不在本阶段实现代码、迁移 Re:Flora 调用点、修改发布产物或提交架构变更；本文档只定义后续实现和验收契约。

## Further Notes

- 本规格综合了架构发现阶段的仓库历史、热点、公共与内部接口、实时约束、测试现状、平台/unsafe 边界、构建发布结构和删除机会；它不依赖临时架构报告才能理解。
- 当前最重要的架构修复顺序是：先建立引擎拥有的自动渲染进度与设备生命周期，再收窄公共 API 和通信契约，随后把空间处理及平台边界放入深模块，最后执行删除和文档收敛。
- 原生 HRTF、Ambisonics 和反射实现已经具有较深的算法边界。本轮重点是减少其上层编排的状态组合和依赖，而不是把算法代码拆成更多小模块。
- 设备恢复是产品硬约束。手动切换系统默认输出、拔出耳机、短暂无设备和设备采样率变化都必须保留 World，并在可用时恢复游戏声音。
- `SpatialFrame` 与 `AcousticSceneSnapshot` 是两种不同更新频率的快照：前者承载每帧监听者和动态 Emitter 状态，后者承载低频变化的只读声学几何。两者不得合并成每帧深拷贝世界的单一结构。
- 历史 API 兼容性不是目标，但每一项删除仍需要删除测试；“私有库”不是进行无收益重写的理由。
